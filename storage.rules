rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Dossier portfolio : lecture publique (images du site), écriture réservée aux utilisateurs connectés
    match /portfolio/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
